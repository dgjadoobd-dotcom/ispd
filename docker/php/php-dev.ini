; PHP Development Override
; Use this file for local development only — NOT for production or staging.
; Mount this file instead of php.ini in docker-compose.dev.yml:
;   volumes:
;     - ./docker/php/php-dev.ini:/usr/local/etc/php/conf.d/99-dev.ini

display_errors = On
display_startup_errors = On
error_reporting = E_ALL
log_errors = On
